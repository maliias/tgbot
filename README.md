# Telegram Payment Bot

Телеграм-бот для автоматизации оплаты зарубежных сервисов с расчетом комиссии и поддержкой нескольких способов оплаты.

## Возможности

### Для пользователей:
- 💳 Оплата зарубежных сервисов через бота
- 📊 Автоматический расчет комиссии (от 3% до 15%)
- 💰 Поддержка 5 способов оплаты:
  - USDT (TRC-20)
  - USDT (BEP-20)
  - Bybit UID
  - Перевод по карте (автоконвертация в RUB)
  - Lolz (с доп. комиссией 4%)
- 👤 Личный профиль со статистикой
- 📦 История заказов
- 🌐 Мультиязычность (RU/EN)
- 🔔 Уведомления о статусе заказа

### Для администраторов:
- 📊 Статистика (день/неделя/месяц)
- 📋 Управление заказами
- ✅ Изменение статуса заказов
- 📢 Рассылка сообщений
- 🔔 Уведомления о новых заказах и оплатах
- ⚙️ Управление реквизитами и настройками

## Структура комиссий

```
S ≤ $10        → 15% (минимум $1.5)
$10 < S ≤ $20  → 12% (минимум $1.5)
$20 < S ≤ $35  → 10%
$35 < S ≤ $50  → 8%
$50 < S ≤ $75  → 6%
$75 < S ≤ $250 → 5%
S > $250       → 3%
```

## Технологический стек

- Python 3.10+
- Aiogram 3.7+ (асинхронный фреймворк для Telegram Bot API)
- PostgreSQL (база данных)
- asyncpg (асинхронный драйвер для PostgreSQL)
- python-dotenv (управление конфигурацией)

## Установка

### 1. Клонирование проекта

```bash
git clone <your-repo-url>
cd tgbot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка PostgreSQL

Установите PostgreSQL и создайте базу данных:

```sql
CREATE DATABASE payment_bot;
```

### 4. Конфигурация

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Заполните `.env` файл:

```env
# Bot Configuration
BOT_TOKEN=your_bot_token_from_@BotFather

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=payment_bot
DB_USER=postgres
DB_PASSWORD=your_password

# Admin Configuration (comma-separated Telegram IDs)
ADMIN_IDS=123456789,987654321

# Admin Notifications Chat ID (can be a group chat)
ADMIN_CHAT_ID=-1001234567890

# Currency Exchange Rate (USD to RUB)
USD_TO_RUB_RATE=95.50
```

### 5. Получение BOT_TOKEN

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и скопируйте полученный токен

### 6. Получение Admin Chat ID

Для получения ID чата для уведомлений админов:

1. Создайте группу в Telegram
2. Добавьте вашего бота в группу
3. Отправьте любое сообщение в группу
4. Используйте бота [@userinfobot](https://t.me/userinfobot) или API для получения Chat ID
5. ID группы начинается с `-100`

### 7. Настройка реквизитов для оплаты

После первого запуска бота, подключитесь к базе данных и обновите настройки:

```sql
-- Обновление реквизитов
UPDATE settings SET value = 'TYourUSDTTRC20Address' WHERE key = 'usdt_trc20_address';
UPDATE settings SET value = '0xYourUSDTBEP20Address' WHERE key = 'usdt_bep20_address';
UPDATE settings SET value = 'YourBybitUID' WHERE key = 'bybit_uid';
UPDATE settings SET value = '1234567890123456' WHERE key = 'card_number';
UPDATE settings SET value = 'YourLolzRequisites' WHERE key = 'lolz_requisites';

-- Обновление инструкций (отображается после оплаты)
UPDATE settings SET value = 'Спасибо за оплату! Ваш заказ обрабатывается. Обычно это занимает 5-30 минут.' WHERE key = 'instruction_text_ru';
UPDATE settings SET value = 'Thank you for payment! Your order is being processed. This usually takes 5-30 minutes.' WHERE key = 'instruction_text_en';

-- Обновление ссылок в разделе "Информация"
UPDATE settings SET value = 'https://t.me/yourchannel' WHERE key = 'info_channel_url';
UPDATE settings SET value = 'https://t.me/yoursupport' WHERE key = 'info_support_url';
UPDATE settings SET value = 'https://yoursite.com/terms' WHERE key = 'info_terms_url';
UPDATE settings SET value = 'https://yoursite.com/refund' WHERE key = 'info_refund_url';
```

## Запуск

```bash
python main.py
```

## Структура проекта

```
tgbot/
├── bot/
│   ├── database/
│   │   ├── __init__.py
│   │   ├── db.py              # Подключение к БД
│   │   └── schema.sql         # SQL схема
│   ├── filters/
│   │   ├── __init__.py
│   │   └── admin.py           # Фильтр для админов
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── common.py          # Общие команды (/start, язык)
│   │   ├── payment.py         # Обработка оплаты
│   │   ├── profile.py         # Профиль и заказы
│   │   └── admin.py           # Админ панель
│   ├── keyboards/
│   │   ├── __init__.py
│   │   └── inline.py          # Inline клавиатуры
│   ├── middlewares/
│   │   ├── __init__.py
│   │   └── user_check.py      # Проверка блокировки
│   ├── models/
│   │   ├── __init__.py
│   │   ├── user.py            # Модель пользователя
│   │   ├── order.py           # Модель заказа
│   │   └── settings.py        # Модель настроек
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── texts.py           # Тексты на разных языках
│   │   └── commission.py      # Расчет комиссии
│   ├── __init__.py
│   └── config.py              # Конфигурация
├── main.py                    # Точка входа
├── requirements.txt           # Зависимости
├── .env.example              # Пример конфигурации
├── .env                      # Конфигурация (не в git)
└── README.md                 # Документация
```

## Использование

### Для пользователей:

1. Запустите бота `/start`
2. Нажмите "💳 Оплата зарубежных сервисов"
3. Введите название сервиса
4. Введите сумму в USD
5. Выберите способ оплаты
6. Получите реквизиты и совершите оплату
7. Нажмите "✅ Я оплатил"
8. Дождитесь подтверждения от администратора

### Для администраторов:

1. Используйте команду `/admin` для доступа к админ панели
2. Доступные функции:
   - **Статистика**: просмотр статистики за разные периоды
   - **Заказы**: управление заказами и изменение статусов
   - **Рассылка**: отправка сообщений всем пользователям

## Статусы заказов

- **PENDING** - Ожидает оплаты (создан, но пользователь еще не подтвердил оплату)
- **PAID_USER** - Оплачен пользователем (требует проверки администратором)
- **COMPLETED** - Успешно выполнен (подтвержден администратором)
- **REJECTED** - Отменен/Не успешен

## Обновление курса валют

Для обновления курса USD/RUB отредактируйте файл `.env`:

```env
USD_TO_RUB_RATE=95.50
```

После изменения перезапустите бота.

## Безопасность

- Все конфиденциальные данные хранятся в `.env` файле
- Добавьте `.env` в `.gitignore`
- Используйте сильные пароли для базы данных
- Регулярно делайте бэкапы базы данных
- Ограничьте доступ к серверу БД

## Бэкап базы данных

```bash
pg_dump -U postgres payment_bot > backup_$(date +%Y%m%d).sql
```

## Восстановление из бэкапа

```bash
psql -U postgres payment_bot < backup_20231215.sql
```

## Решение проблем

### Бот не запускается

1. Проверьте правильность BOT_TOKEN в `.env`
2. Убедитесь, что PostgreSQL запущен
3. Проверьте параметры подключения к БД

### Ошибки базы данных

1. Убедитесь, что база данных создана
2. Проверьте права доступа пользователя PostgreSQL
3. Проверьте логи в консоли

### Бот не отвечает на команды

1. Убедитесь, что бот запущен
2. Проверьте, не заблокирован ли пользователь
3. Проверьте логи на наличие ошибок

## Развитие проекта

Возможные улучшения:

- [ ] Автоматическая проверка платежей через API
- [ ] Интеграция с payment gateway
- [ ] Система скидок для постоянных клиентов
- [ ] Реферальная программа
- [ ] Экспорт статистики в Excel/CSV
- [ ] Автоматическое обновление курса валют через API
- [ ] Поддержка дополнительных валют
- [ ] Telegram Web App для оплаты

## Лицензия

Проект создан для коммерческого использования. Все права защищены.

## Поддержка

По всем вопросам обращайтесь к разработчику.
